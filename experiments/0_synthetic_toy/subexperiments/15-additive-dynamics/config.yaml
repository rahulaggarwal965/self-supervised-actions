# reproduce (label-free, step=20, K=6)
model: minimal_invariant_hires_add   # additive dynamics; baseline compare: minimal_invariant_hires_proj
loss: ssl_cf
overrides:
  - model.num_codes=6
  - data.counterfactuals=true
  - data.env.step=20
